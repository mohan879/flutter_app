name: core
description: Core utilities package for monorepo isolation example.
version: 0.1.0
publish_to: none
environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  feature_home:
    path: ../feature_home

dev_dependencies:
  dart_code_metrics: ^5.7.6
