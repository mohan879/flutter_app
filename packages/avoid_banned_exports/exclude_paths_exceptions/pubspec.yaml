name: abe_exclude_paths_exceptions
description:
  Example showing exclude-paths configuration for avoid-banned-exports.
version: 0.1.0
publish_to: none
environment:
  sdk: '>=3.2.0 <4.0.0'

dev_dependencies:
  dart_code_metrics: ^5.7.6
