name: clean_architecture_boundaries
description:
  Example demonstrating avoid-banned-types for clean architecture layer
  enforcement
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
