name: exclude_paths_exceptions
description:
  Example demonstrating avoid-banned-types exclude-paths for targeted exceptions
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.3.0
