// ❌ Old file - should be deleted and use git history instead
class AuthServiceOld {
  Future<bool> login(String email, String password) async {
    // Old implementation that was replaced
    return false;
  }
}
