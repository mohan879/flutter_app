name: exclude_paths_exceptions
description: Example demonstrating exclude-paths usage with avoid-banned-imports
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dev_dependencies:
  dcm: any
