name: dependency_injection_control
description: Example demonstrating avoid-banned-types for DI type control
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  get_it: ^7.6.0
