name: platform_specific_imports
description:
  Example demonstrating platform-specific import control with
  avoid-banned-imports
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dev_dependencies:
  dcm: any
