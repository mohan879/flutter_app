name: severity_configuration
description:
  Example demonstrating severity configuration for CI vs local development
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies: {}
