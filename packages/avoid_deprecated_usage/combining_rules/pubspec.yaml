name: combining_rules
description:
  Example demonstrating combining avoid-deprecated-usage with banned-usage and
  avoid-banned-types
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
